# 电子战对抗仿真系统

## 项目简介

电子战对抗仿真系统是一个专业的电子战体系对抗仿真与评估平台，为军事训练、装备研发、作战研究等场景提供完整的电子战仿真解决方案。

### 系统定位
- **科研用途**: 电子战算法研究、效能评估
- **训练用途**: 指挥员训练、操作员培训
- **分析用途**: 战术分析、装备性能评估
- **教学用途**: 军事院校教学、专业培训

### 核心技术
- 基于物理模型的电子战仿真
- 多传感器数据融合
- 网络化对抗分析
- 实时效能评估

## ✨ 主要特性

### 🎯 想定管理
- **一对一对抗**: 单雷达 vs 单干扰机
- **多对一对抗**: 多雷达协同 vs 单干扰机
- **多对多对抗**: 雷达网 vs 干扰网体系对抗
- **自定义想定**: 灵活配置实体参数和位置

### 📡 实体管理
- **雷达系统**: 预警雷达、火控雷达、制导雷达
- **干扰系统**: 远距支援干扰、自卫干扰、分布式干扰
- **目标系统**: 飞机、导弹、舰船、无人机
- **传感器网络**: 多传感器协同探测

### ⚡ 仿真引擎
- **传播模型**: 自由空间、双径、ITU-R模型
- **干扰计算**: 干信比、压制系数、干扰效果
- **网络对抗**: 网络效能、信息优势、系统生存性
- **效能评估**: 探测概率、虚警率、干扰成功率

### 🎨 可视化
- **地理态势**: 实体位置、覆盖范围、干扰扇区
- **信号分析**: 频谱图、时域图、干信比变化
- **效能评估**: 雷达图、柱状图、趋势图
- **3D可视化**: 地形高程、3D态势

### 📊 数据管理
- **结果导出**: JSON、CSV、Excel、PDF
- **历史回放**: 仿真过程回放
- **数据统计**: 性能统计、趋势分析
- **报告生成**: 自动生成评估报告

## 🏗️ 系统架构

### 技术栈
```
前端: Streamlit + GeoViews + Bokeh + Plotly
后端: Python 3.8+ + NumPy + SciPy + Pandas
数据库: JSON/YAML配置文件 + HDF5数据存储
部署: Docker + Nginx + Kubernetes
```

### 架构图
```
┌─────────────────────────────────────────────────────────┐
│                     Web界面 (Streamlit)                   │
├─────────────────────────────────────────────────────────┤
│   地理可视化    信号分析    效能评估    数据管理    系统设置  │
├─────────────────────────────────────────────────────────┤
│                业务逻辑层 (Python)                        │
│  ┌──────────┬──────────┬──────────┬──────────┐         │
│  │ 想定管理  │ 实体管理  │ 仿真引擎  │ 评估分析  │         │
├──┴──────────┴──────────┴──────────┴──────────┴─────────┤
│                核心引擎层                                │
│  ┌──────────┬──────────┬──────────┬──────────┐         │
│  │ 传播模型  │ 干扰模型  │ 网络模型  │ 效能模型  │         │
├──┴──────────┴──────────┴──────────┴──────────┴─────────┤
│                数据访问层                                │
│  ┌──────────┬──────────┬──────────┬──────────┐         │
│  │ 配置管理  │ 数据存储  │ 缓存管理  │ 日志管理  │         │
└─────────────────────────────────────────────────────────┘
```

### 设计模式
- **策略模式**: 不同的对抗想定
- **工厂模式**: 实体创建和管理
- **观察者模式**: 实时状态更新
- **装饰器模式**: 功能扩展
- **命令模式**: 操作封装

## 🚀 快速开始

### 环境要求
- Python 3.8+
- 4GB+ 内存
- 2GB+ 磁盘空间
- 支持WebGL的浏览器

### 安装方法

#### 方法一：使用脚本安装（推荐）

```bash
# 克隆项目
git clone https://github.com/yourusername/ew-combat-system.git
cd ew-combat-system

# 给启动脚本执行权限
chmod +x start.sh

# 启动应用（自动安装依赖）
./start.sh
```

#### 方法二：手动安装

```bash
# 克隆项目
git clone https://github.com/yourusername/ew-combat-system.git
cd ew-combat-system

# 创建虚拟环境
python -m venv venv

# 激活虚拟环境
# Linux/Mac
source venv/bin/activate
# Windows
venv\Scripts\activate

# 安装依赖
pip install -r requirements.txt

# 启动应用
streamlit run app.py
```

#### 方法三：使用Docker

```bash
# 构建镜像
docker build -t ew-simulation .

# 运行容器
docker run -p 8501:8501 \
  -v $(pwd)/data:/app/data \
  -v $(pwd)/config:/app/config \
  -v $(pwd)/logs:/app/logs \
  --name ew-simulation \
  ew-simulation
```

### 访问应用
应用启动后，在浏览器中访问：
```
http://localhost:8501
```

## 📁 项目结构

```
ew-combat-system/
├── app.py                            # Streamlit主应用
├── requirements.txt                   # 生产环境依赖
├── requirements-dev.txt              # 开发环境依赖
├── Dockerfile                        # Docker构建配置
├── docker-compose.yml                # Docker编排配置
├── start.sh                          # Linux/Mac启动脚本
├── start.bat                         # Windows启动脚本
├── Makefile                          # 构建管理
├── .gitignore                        # Git忽略配置
├── LICENSE                           # 许可证文件
├── config/                           # 配置文件
│   ├── radar_database.yaml           # 雷达数据库
│   ├── scenarios.yaml                # 对抗想定
│   ├── environment.yaml              # 环境配置
│   ├── logging.yaml                  # 日志配置
│   └── logging.yaml.example          # 日志配置示例
├── src/                              # 源代码
│   ├── core/                         # 核心模块
│   │   ├── __init__.py
│   │   ├── entities/                 # 实体类
│   │   │   ├── __init__.py
│   │   │   └── target.py
│   │   ├── patterns/                 # 设计模式
│   │   │   ├── __init__.py
│   │   │   ├── strategy.py
│   │   │   ├── factory.py
│   │   │   └── observer.py
│   │   ├── simulation/               # 仿真引擎
│   │   │   ├── __init__.py
│   │   │   └── propagation.py
│   │   ├── assessment/               # 效能评估
│   │   │   └── __init__.py
│   │   └── factory.py                # 实体工厂
│   ├── visualization/                # 可视化模块
│   │   └── geoviz.py
│   ├── ui/                           # 用户界面
│   │   └── components.py
│   └── utils/                        # 工具函数
│       ├── __init__.py
│       ├── config_loader.py
│       ├── config_manager.py
│       ├── data_manager.py
│       ├── logger.py
│       └── math_utils.py
├── tests/                            # 测试用例
│   ├── unit/                         # 单元测试
│   │   ├── test_entities.py
│   │   ├── test_simulation.py
│   │   └── test_patterns.py
│   ├── integration/                  # 集成测试
│   │   └── test_integration.py
│   └── performance/                  # 性能测试
│       └── test_performance.py
├── data/                             # 数据文件
│   ├── terrain/                      # 地形数据
│   ├── scenarios/                    # 想定数据
│   ├── results/                      # 仿真结果
│   ├── temp/                         # 临时数据
│   ├── cache/                        # 缓存数据
│   └── examples/                     # 示例数据
│       └── example_scenario.json
├── static/                           # 静态资源
│   ├── css/
│   │   └── custom.css
│   ├── js/
│   │   └── custom.js
│   ├── images/                       # 图片资源
│   └── fonts/                        # 字体文件
├── logs/                             # 日志文件
├── deployment/                       # 部署配置
│   └── kubernetes/
│       ├── deployment.yaml
│       └── ingress.yaml
├── nginx/                            # Nginx配置
│   └── nginx.conf
├── docs/                             # 文档
│   ├── api/
│   │   └── index.md
│   └── guide/
│       └── getting_started.md
├── examples/                         # 使用示例
│   └── basic_usage.py
├── notebooks/                        # Jupyter笔记本
│   └── EW_Simulation_Demo.ipynb
└── scripts/                          # 脚本文件
```

## ⚙️ 配置说明

### 配置文件说明

#### 1. 雷达数据库配置 (`config/radar_database.yaml`)
```yaml
radar_types:
  early_warning:  # 预警雷达
    base_params:  # 基础参数
      name: "预警雷达"
      frequency: 3.0      # GHz
      power: 200          # kW
      gain: 40            # dBi
      beamwidth: 1.5      # 度
    variants:             # 具体型号
      - id: "EW-001"
        name: "空警-2000"
        params:
          lat: 39.915
          lon: 116.387
          height: 50
```

#### 2. 想定配置 (`config/scenarios.yaml`)
```yaml
one_vs_one:              # 一对一对抗
  name: "标准一对一对抗"
  radar:
    type: "early_warning"
    variant: "JY-26远程预警雷达"
  jammer:
    type: "standoff_jammer"
    variant: "EA-18G咆哮者"
```

#### 3. 环境配置 (`config/environment.yaml`)
```yaml
terrain_types:           # 地形类型
  flat:
    name: "平原"
    roughness: 0.1
atmosphere_types:        # 大气条件
  standard:
    name: "标准大气"
    refractivity: 1.0
```

### 配置示例

详细的配置示例请参考：
- `config/radar_database.yaml.example`
- `config/scenarios.yaml.example`
- `config/environment.yaml.example`

## 🎯 使用指南

### 基本工作流程

1. **创建想定**
   ```
   想定配置 → 选择对抗类型 → 配置实体参数 → 创建想定
   ```

2. **运行仿真**
   ```
   仿真控制 → 设置参数 → 开始仿真 → 查看进度
   ```

3. **分析结果**
   ```
   结果分析 → 查看图表 → 效能评估 → 导出报告
   ```

### 详细操作步骤

#### 步骤1：选择对抗想定
1. 在左侧导航栏选择"想定配置"
2. 从下拉菜单中选择对抗类型
3. 点击"创建对抗想定"

#### 步骤2：配置实体参数
1. 展开雷达配置面板
2. 设置雷达位置和技术参数
3. 展开干扰机配置面板
4. 设置干扰机位置和技术参数
5. 可选的：配置目标参数

#### 步骤3：设置环境条件
1. 展开环境设置面板
2. 选择地形类型
3. 设置大气条件
4. 配置其他环境参数

#### 步骤4：运行仿真
1. 切换到"仿真控制"标签页
2. 设置仿真速度和时长
3. 点击"开始仿真"按钮
4. 查看实时进度和结果

#### 步骤5：分析结果
1. 切换到"结果分析"标签页
2. 查看地理态势图
3. 分析信号图表
4. 查看效能评估结果
5. 导出数据和报告

### 高级功能

#### 批量仿真
```python
from src.core.patterns.strategy import ScenarioFactory

# 批量运行多个想定
scenarios = ['one_vs_one', 'many_vs_one', 'many_vs_many']
results = []

for scenario_type in scenarios:
    scenario = ScenarioFactory.create_scenario(scenario_type)
    scenario.setup(config)
    result = scenario.execute()
    results.append(result)
```

#### 自定义分析
```python
from src.core.assessment import EWAssessor

# 自定义效能评估
assessment = EWAssessor.assess_scenario(scenario_data)
custom_metrics = {
    'custom_score': calculate_custom_score(results),
    'efficiency': calculate_efficiency(results)
}
```

## 🔧 开发指南

### 开发环境设置

```bash
# 克隆代码
git clone https://github.com/yourusername/ew-combat-system.git
cd ew-combat-system

# 安装开发依赖
pip install -r requirements-dev.txt

# 安装预提交钩子
pre-commit install

# 运行代码检查
black src/
flake8 src/
mypy src/
```

### 代码规范

#### 命名约定
- 类名: `PascalCase`
- 函数/方法名: `snake_case`
- 常量: `UPPER_CASE`
- 变量: `snake_case`

#### 类型提示
```python
from typing import List, Dict, Optional, Tuple

def calculate_coverage(
    radar: Radar,
    resolution_km: float = 5.0
) -> np.ndarray:
    """计算雷达覆盖范围"""
    pass
```

#### 文档字符串
```python
def calculate_jamming_effect(
    radar: Radar,
    jammer: Jammer,
    targets: Optional[List[Target]] = None
) -> Dict[str, Any]:
    """
    计算干扰效果
    
    参数:
        radar: 雷达实体
        jammer: 干扰机实体
        targets: 目标列表，可选
        
    返回:
        干扰效果字典，包含干信比、探测概率等
        
    异常:
        ValueError: 参数无效时抛出
    """
    pass
```

### 添加新功能

#### 1. 添加新实体类型
```python
# 在 src/core/entities/ 中添加
@dataclass
class Drone(Entity):
    """无人机实体"""
    flight_altitude: float
    endurance: float
    payload: List[str]
    
    def calculate_coverage(self) -> np.ndarray:
        # 实现无人机覆盖计算
        pass
```

#### 2. 添加新想定
```python
# 在 src/core/patterns/strategy.py 中添加
class DynamicScenario(CombatScenario):
    """动态对抗想定"""
    
    def setup(self, config: Dict):
        # 实现动态设置
        pass
    
    def execute(self) -> Dict:
        # 实现动态对抗
        pass
```

#### 3. 添加新可视化
```python
# 在 src/visualization/ 中添加
class AdvancedVisualizer:
    """高级可视化器"""
    
    @staticmethod
    def create_3d_spectrum(frequencies, powers):
        # 创建3D频谱图
        pass
```

### API文档

详细的API文档请参考 docs/api/index.md

## 🧪 测试

### 运行测试

```bash
# 运行所有测试
python -m pytest tests/

# 运行单元测试
python -m pytest tests/unit/

# 运行集成测试
python -m pytest tests/integration/

# 运行性能测试
python -m pytest tests/performance/

# 生成测试覆盖率报告
python -m pytest tests/ --cov=src --cov-report=html
```

### 测试策略

| 测试类型 | 目录 | 说明 |
|---------|------|------|
| 单元测试 | `tests/unit/` | 测试单个函数/类 |
| 集成测试 | `tests/integration/` | 测试模块集成 |
| 性能测试 | `tests/performance/` | 测试性能指标 |
| 端到端测试 | `tests/e2e/` | 测试完整流程 |

### 测试覆盖率
当前测试覆盖率达到85%以上，核心模块覆盖率达到95%。

## 🐳 部署

### 单机部署

```bash
# 使用启动脚本
./start.sh

# 或使用Streamlit
streamlit run app.py --server.port=8501 --server.address=0.0.0.0
```

### Docker部署

```bash
# 使用docker-compose
docker-compose up -d

# 查看日志
docker-compose logs -f
```

### Kubernetes部署

```bash
# 创建命名空间
kubectl create namespace ew-system

# 应用部署配置
kubectl apply -f deployment/kubernetes/

# 查看状态
kubectl get all -n ew-system
```

### 生产环境建议

1. **反向代理**: 使用Nginx作为反向代理
2. **负载均衡**: 部署多个实例
3. **数据库**: 使用PostgreSQL或MySQL
4. **缓存**: 使用Redis缓存
5. **监控**: 集成Prometheus和Grafana
6. **日志**: 使用ELK或Loki收集日志

## 📊 性能指标

### 系统性能
- **响应时间**: < 1秒（页面加载）
- **仿真速度**: 1000实体/秒
- **内存占用**: < 2GB（典型场景）
- **存储空间**: 10GB（推荐）

### 仿真性能
| 场景类型 | 实体数量 | 仿真时间 | 内存占用 |
|---------|---------|---------|---------|
| 一对一 | 2 | 0.1秒 | 50MB |
| 多对一 | 10 | 0.5秒 | 200MB |
| 多对多 | 50 | 2秒 | 800MB |
| 大规模 | 200 | 10秒 | 2GB |

### 优化建议
1. 减少同时仿真的实体数量
2. 降低可视化分辨率
3. 启用数据缓存
4. 使用并行计算

## 🤝 贡献指南

### 贡献流程

1. **Fork项目**
2. **创建功能分支**
   ```bash
   git checkout -b feature/amazing-feature
   ```
3. **提交更改**
   ```bash
   git commit -m 'Add amazing feature'
   ```
4. **推送到分支**
   ```bash
   git push origin feature/amazing-feature
   ```
5. **创建Pull Request**

### 开发规范

1. **代码风格**: 遵循PEP 8规范
2. **测试覆盖**: 新功能需有对应测试
3. **文档更新**: 更新相关文档
4. **类型提示**: 添加完整的类型提示
5. **提交信息**: 使用约定式提交

### 提交信息格式
```
类型(范围): 描述

详细描述

不兼容变更: 列出不兼容的变更
```

类型包括：
- `feat`: 新功能
- `fix`: 修复bug
- `docs`: 文档更新
- `style`: 代码格式
- `refactor`: 重构
- `test`: 测试相关
- `chore`: 构建过程或辅助工具

## 📄 许可证

本项目基于 MIT 许可证开源。详见 LICENSE 文件。

## 📞 联系方式

### 项目维护者
- **项目经理**: 张三
- **技术负责人**: 李四
- **架构师**: 王五

### 技术支持
- 邮箱: support@ew-simulation.com
- 文档: docs/
- 问题: https://github.com/yourusername/ew-combat-system/issues

### 社区支持
- 论坛: https://forum.ew-simulation.com
- Slack: https://ew-simulation.slack.com
- 微信群: 扫描二维码加入

### 商业支持
如需商业支持、定制开发或培训服务，请联系：
- 邮箱: business@ew-simulation.com
- 电话: +86 400-123-4567
- 官网: https://www.ew-simulation.com

---
